<Window x:Class="EgeGenerator.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ввод количества вариантов" 
        Height="216" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Сколько вариантов хотите сгенерировать?" 
                   FontSize="16"
                   Margin="0,0,0,15"/>
        <TextBox x:Name="txtInput" 
                 Grid.Row="1" 
                 FontSize="18"
                 Height="40"
                 TextAlignment="Center"/>
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="ОК" 
                    Width="80" 
                    Height="35"
                    IsDefault="True"
                    Click="BtnOk_Click"/>
            <Button Content="Отмена" 
                    Width="80" 
                    Height="35"
                    Margin="10,0,0,0"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>